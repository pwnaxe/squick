# squick (Python)

Python bindings for [Squick](../../readme.md).

```python
import squick
print(squick.scan("."))
```

Build locally (requires Rust toolchain and `maturin`):

```bash
pip install maturin
maturin develop --release
```

Licensed under the Apache License 2.0.
Copyright 2026 Horizon LLC.
